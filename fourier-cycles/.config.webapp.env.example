# Fourier Cycles WebApp Configuration Defaults

# Local port to bind the UI (accessible via Tailscale or localhost)
FOURIER_UI_HOST_PORT=3010
FOURIER_UI_HOST_BIND_ADDRESS=127.0.0.1

# Default output directory mounted as read-only to APIs and UI
# Usually matches the path from main fourier compose file
FOURIER_OUTPUT_DIR_HOST=/home/wasti/ai_stack/fourier-cycles/output
