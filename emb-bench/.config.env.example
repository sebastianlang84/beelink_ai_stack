# emb-bench Config (Non-Secrets)
#
# emb-bench liest die meisten Defaults aus YAML/CLI; diese Datei ist optional.
# Set these to your host user/group IDs (`id -u`, `id -g`).
# Defaults below are common on Linux, but may not match your system.
DOCKER_UID=1000
DOCKER_GID=1000
