# Shared Config (Non-Secrets)
#
# Create `.config.env` from this file and keep it local (gitignored).
# This file should contain host-/setup-specific values that are not secrets.

# Common
TZ=Europe/Vienna

# Open WebUI (host mapping)
OPEN_WEBUI_IMAGE_TAG=0.8.1
OPEN_WEBUI_BIND_ADDRESS=127.0.0.1
OPEN_WEBUI_HOST_PORT=3000
OLLAMA_BASE_URL=

# MCP Transcript Miner (host paths)
# Output root (recommended: /srv/ai-stack/transcript-miner/output; legacy default below)
TRANSCRIPT_MINER_OUTPUT_ROOT_HOST=/home/wasti/ai_stack_data/transcript-miner/output
# Host dir mounted into the container at `/host_secrets` (default: repo root via `..`)
AI_STACK_SECRETS_DIR_HOST=..

# Transcript Miner (proxy config, non-secrets)
YOUTUBE_PROXY_MODE=none
YOUTUBE_PROXY_FILTER_IP_LOCATIONS=

# Qdrant (host mapping)
QDRANT_BIND_ADDRESS=127.0.0.1
QDRANT_HTTP_PORT=6333
